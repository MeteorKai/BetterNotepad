# ============================================================
#  BetterNotepad —— 开源前必须遵守的忽略规则
#  ⚠️ 本文件是「密钥不外泄」的第一道防线，请勿轻易删改。
# ============================================================

# ---- 依赖 ----
node_modules/

# ---- 构建产物（体积大且可重建） ----
dist/
src-tauri/target/

# ---- 密钥与凭据：绝对不要提交 ----
# updater 签名私钥本体存放在仓库之外（%USERPROFILE%\.tauri\），
# 下面这些规则是「万一有人把密钥复制进项目」时的兜底。
*.key
*.key.pub
*.pem
*.p12
*.pfx
*.jks
*.keystore
*.sig
.env
.env.*
*.password.txt

# ---- 本地工具状态 ----
# ⚠️ .workbuddy/memory/ 的记录里出现过「签名私钥口令」的明文，
# 一旦提交就等于把口令公开；.omc/ 与 .claude/ 是会话记录与权限清单，
# 还含本机绝对路径（会暴露 Windows 用户名）。
# 注意：忽略 ≠ 删除，这些目录仍保留在本地。
.workbuddy/
.omc/
.claude/

# ---- 临时草稿 / 日志 ----
__*.txt
*.log

# ---- 操作系统 / 编辑器 ----
Thumbs.db
Desktop.ini
.DS_Store
.idea/
.vscode/
